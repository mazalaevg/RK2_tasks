#include <iostream>
#include "tasks_rk2.h"
int main()
{
  //  srand(time(NULL));
 /*   int *ar=new int [10];
    for (int i = 0; i < 10; ++i) {
        ar[i]=i;
    }
    for (int i = 0; i < 10; ++i) {
        for (int j = 0; j < 10; ++j) {
            if(ar[j]>ar[j+1])
                swap(ar[j],ar[j+1]);
        }
    }
    for (int i = 0; i < 10 ; ++i) {
        cout << ar[i] <<" ";
    }

    binSearch(ar,10,6);

*/

    return 0;
}
